import { GridType, TileType, SpeedRate } from '../../../utils/types';

export const runAStarAlgorithm =async({
    grid,
    startTile,
    endTile,
    speedRate
}:{
    grid:GridType,
    startTile:TileType,
    endTile:TileType,
    speedRate:SpeedRate,
})=>{

}